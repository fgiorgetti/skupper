This sample expects that secrets are copied from west to the east site
using ACM (or some similar tool), so that the secret name is all it needs
to get sites linked.

Services are exposed by selector only.